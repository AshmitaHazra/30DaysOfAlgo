using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace RepeatingAndMissing
{
    class RepeatingAndMissing
    {
        public static void Main(string[] args)
        {
            int[] arr = { 1, 3, 4, 5, 1, 6, 2 };

            int n = arr.Length;
            getTwoElements(arr, n);
            Console.Write(" The missing element is " +x + "and the repeating number is "+ y);
        }
        public static int getTwoElements(int[] arr, int size)
        {

        }
    }
}
